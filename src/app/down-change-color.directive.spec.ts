import { DownChangeColorDirective } from './down-change-color.directive';

describe('DownChangeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DownChangeColorDirective();
    expect(directive).toBeTruthy();
  });
});
